---
import { contact } from "../data";
---

<div class="mt-auto">
  <section class="bg-muted text-muted-foreground px-4 py-8">
    <div
      class="max-w-6xl mx-auto flex flex-col gap-4 justify-center items-center"
    >
      <h2 class="text-2xl font-bold">Work with me</h2>
      <p>Contact me today to discuss your project and get a free quote.</p>
      <a
        href="/contact"
        class="bg-secondary text-secondary-foreground px-4 py-2 rounded-md"
        >Contact me</a
      >
    </div>
  </section>
  <footer class="bg-foreground text-background px-4 py-8 text-sm">
    <div
      class="max-w-6xl mx-auto flex items-center flex-col md:flex-row justify-center md:justify-between gap-4"
    >
      <div class="flex items-center gap-2">
        <p>
          <a href={`tel:${contact.phone}`}>{contact.phoneFormatted}</a>
        </p>
        |
        <p>
          <a href={`mailto:${contact.email}`}>{contact.email}</a>
        </p>
      </div>
      <p>Carpentry &amp; Handyman Services - Capel-le-Ferne, Kent</p>
    </div>
  </footer>
</div>
